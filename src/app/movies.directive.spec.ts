import { MoviesDirective } from './movies.directive';

describe('MoviesDirective', () => {
  it('should create an instance', () => {
    const directive = new MoviesDirective();
    expect(directive).toBeTruthy();
  });
});
